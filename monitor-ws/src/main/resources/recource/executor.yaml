get:
  tags:
    - executor
  operationId: getTests
  responses:
    200:
      description: Ok
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "../schemas.yaml#/TestRef"
